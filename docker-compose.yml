services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: biomix-dev
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - DB_PATH=biomix.db
      - PORT=8080
    restart: unless-stopped
